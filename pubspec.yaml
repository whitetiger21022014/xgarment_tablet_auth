
name: xgarment_tablet_auth
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  mobile_scanner: ^4.0.1
  permission_handler: ^11.3.1
  flutter_inappwebview: ^6.0.0
  http: ^1.2.1
  shared_preferences: ^2.2.2
  device_info_plus: ^10.1.0
  # Thêm các package khác bạn thực sự cần

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/config.json
    - assets/companyDefault_logo.png
